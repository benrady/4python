

def test_set_union():
    """The union() method combines two sets, resulting in a set with the unique values of both"""
    simple_set = None  # Replace this
    assert simple_set == {1, 2, 3}.union({4, 3, 2})
