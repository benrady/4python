

def test_sets():
    """The intersection() method combines two sets using only the values that both sets have"""
    simple_set = None  # Replace this
    assert simple_set == {1, 2, 3}.union({2, 3, 4})
    assert {2, 3} == simple_set.intersection({2, 3, 5})
